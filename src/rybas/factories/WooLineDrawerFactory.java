package rybas.factories;

import rybas.linedrawers.LineDrawer;


public class WooLineDrawerFactory implements LineDrawerFactory {
    @Override
    public LineDrawer createLineDrawer(Object drawer) {
        return null;
    }
}
